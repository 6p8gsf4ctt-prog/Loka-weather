LOKA V24 — CORRECTIF BLOC 9.2.1
================================

OBJECTIF
--------
Corriger l'erreur mobile Safari affichée dans Calibration V24 :
"SyntaxError: The string did not match the expected pattern."

FICHIERS
--------
REMPLACER :
- src/ui/dashboard.ts
- src/storage/shadowMetrics.ts

AUCUNE MIGRATION D1.

CORRECTIONS
-----------
1. Admin mobile :
   - les réponses API sont d'abord lues comme texte ;
   - elles sont ensuite parsées explicitement en JSON ;
   - si Cloudflare/D1 renvoie du texte ou une page d'erreur, l'Admin affiche
     maintenant le vrai statut HTTP et le début de la réponse au lieu d'un
     SyntaxError Safari opaque.

2. Requête D1 metrics :
   - suppression du modificateur SQLite dynamique :
       date('now', '-' || ? || ' days')
   - la date de coupure est désormais calculée en JavaScript au format YYYY-MM-DD
     puis liée comme paramètre simple :
       forecast_date >= ?

Cette forme est plus robuste avec D1 et évite les différences de parsing.

SECURITE
--------
- forecast.scene inchangé
- Legacy toujours officiel
- aucune écriture supplémentaire
- aucune migration
- /instagram et /instagram24 inchangés

TEST
----
Après déploiement :
1. Ouvrir /admin
2. Saisir ADMIN_TOKEN
3. Appuyer sur "Analyser Shadow V24"

Résultat attendu :
- soit les cartes de calibration s'affichent ;
- soit un message d'erreur précis "HTTP ..." apparaît, exploitable immédiatement.

Si les cartes s'affichent, Bloc 9 est considéré techniquement opérationnel et
nous pouvons passer au Bloc 10.
