LOKA V24 — BLOC 12.1

OBJECTIF
Connecter réellement le sélecteur au pipeline de chaque génération sans
permettre encore à V24 de devenir officiel.

AJOUTER
- src/engine/engineRuntime.ts

REMPLACER
- src/engine/engineMode.ts
- src/pipeline.ts
- src/index.ts
- src/ui/dashboard.ts

Le ZIP inclut publicPreview.ts et editorial24.ts pour cohérence.
Aucune migration D1.

NOUVEAU FLUX
Legacy + V24 Shadow
→ engine_control
→ readiness historique
→ resolveSceneEngineMode()
→ applyEngineResolutionToForecast()
→ shadow_history
→ forecasts

VERROU ABSOLU
effectiveProduction = LEGACY

engineRuntime.ts capture puis restaure systématiquement :
- forecast.scene
- subtitle
- summaryLines
- mainVerdict
- rainVerdict
- notableEvent

Une erreur de control/readiness/payload V24 ne bloque jamais la production
Legacy.

READINESS
Le run courant n'est évalué qu'après les générations déjà archivées :
readinessExcludesCurrentGeneration = true.
Une génération ne peut donc pas contribuer à sa propre autorisation.

DIAGNOSTICS
Chaque nouvelle génération contient diagnostics.sceneEngine avec :
- version 12.1.0
- connectedInPipeline = true
- requested
- effectiveProduction = LEGACY
- previewEnabled
- readiness
- reason
- controlAvailable
- v24CandidateAvailable

ADMIN
Après une nouvelle génération :
- Sélecteur dans le pipeline = CONNECTÉ
- Dernière génération effective = LEGACY
- Raison pipeline visible

TEST MOBILE
1. Garder V24_PREVIEW actif.
2. Déployer Bloc 12.1.
3. Générer une nouvelle météo.
4. Contrôler le moteur.
5. Vérifier :
   Production = LEGACY
   demandé = V24_PREVIEW
   Sélecteur pipeline = CONNECTÉ
   Dernière génération effective = LEGACY
6. /preview24 et /instagram24-preview restent disponibles.
7. forecast.scene reste Legacy.
8. Test demande V24 production reste refusé.
9. Revenir à Legacy coupe les previews comme avant.

Bloc 12.2 pourra ensuite construire l'autorisation réelle avec double
confirmation et snapshot du readiness, mais elle restera refusée tant que
READY_CANDIDATE n'est pas atteint.
